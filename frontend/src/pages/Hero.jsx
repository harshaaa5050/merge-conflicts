import HeroComponent from "@/components/mvpblocks/gradient-hero";
import React from "react";

const HeroPage = () => {
  return (
    <section id="hero-section">
      <HeroComponent />
    </section>
  );
};

export default HeroPage;
